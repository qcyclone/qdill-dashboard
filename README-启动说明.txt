启动方式（解决数据源拿不到）

1. 双击运行 `start-server.bat`
2. 在浏览器打开 `http://127.0.0.1:8765`
3. 点击“更新所有数据”

说明：
- 数据由本地 `server.ps1` 抓取东方财富/天天基金，再返回给前端。
- 前端直接访问外站经常被跨域/反爬拦截，本地服务可规避这个问题。
- 保持 `start-server.bat` 窗口开启，页面更新才能正常工作。
